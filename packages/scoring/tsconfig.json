{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // No DOM, no Node types. If a change here needs either, it does not
    // belong in this package - it belongs in @pps/features or the app.
    "lib": ["ES2022"],
    "types": [],
    "outDir": "dist",
    "rootDir": "src"
  },
  "include": ["src/**/*.ts"],
  "exclude": ["src/**/*.test.ts"]
}
